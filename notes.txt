todo
    add BE to modifier
        for fight talents, is BE even considered? it's not listed in xml file
    extra test for reading/writing
    attack, evade, parade rolls (need BE first)

    (attr,skill,spell change from class into other data format?)
    (calculate probabilities?)

old version using pygame: when converting to exe (using windows), 'freesansbold.ttf' wasn't included automatically. had to manually copy it into the folder.

converted using 'pyinstaller main.py'
